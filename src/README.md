# src/

This folder is a placeholder for shared/reusable logic if the project is later
split across multiple .ino/.h files (e.g. a `ParkingSlot.h` class). For the
current beginner-friendly version, all logic lives directly in the sketches
under `arduino_code/`.
